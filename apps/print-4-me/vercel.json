{
  "framework": "nextjs",
  "buildCommand": "pnpm run build",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "ignoreCommand": "if git diff --quiet 'HEAD^' HEAD -- ./; then exit 1; else exit 0; fi"
}
