// @stratos/utils - Shared utilities for Stratos apps

// String utilities
export { cn, capitalize, truncate, slugify } from "./string";

// Date utilities
export { formatDate, formatRelativeTime, isExpired } from "./date";

// Validation utilities
export { isEmail, isPhone, isUrl, isEmpty } from "./validation";

// Object utilities
export { pick, omit, deepMerge } from "./object";

// Array utilities
export { unique, groupBy, chunk } from "./array";
