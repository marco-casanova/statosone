export const metadata = {
  title: "Developer Profile - StratosTalent",
  description: "View developer profile, projects, and reviews.",
};

export default function DeveloperProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
